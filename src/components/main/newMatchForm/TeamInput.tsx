const TeamInput: React.FC = (props) => {
  return (
    <section style={{ display: 'flex' }}>
      {/* <PlayerInput players={team1} />
      <PlayerInput players={team1} /> */}
    </section>
  );
};

export default TeamInput;
